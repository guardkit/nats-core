# Assumptions manifest for nats-configuration.feature
# Generated by /feature-spec

feature: "NATS Configuration"
generated: "2026-04-08T00:00:00Z"
stack: "python"
review_required: false

assumptions:
  - id: "ASSUM-001"
    scenario: "Invalid URL scheme is rejected"
    assumption: "URL must start with nats:// or tls:// per nats-py convention"
    confidence: medium
    basis: "nats-py convention; nats:// for plaintext, tls:// for TLS connections"
    human_response: "confirmed"

  - id: "ASSUM-002"
    scenario: "Empty client name is rejected"
    assumption: "Client name is required and must not be empty"
    confidence: medium
    basis: "NATS monitoring dashboard uses client name for identification and observability"
    human_response: "confirmed"

  - id: "ASSUM-003"
    scenario: "User without password is rejected"
    assumption: "User and password must be provided together; partial auth is a misconfiguration"
    confidence: low
    basis: "nats-py requires both user and password for basic auth; partial credentials are likely a configuration error"
    human_response: "confirmed"

  - id: "ASSUM-004"
    scenario: "Configuration loads from dotenv file"
    assumption: "NATSConfig reads from .env files via pydantic-settings default behaviour"
    confidence: medium
    basis: "pydantic-settings BaseSettings supports env_file in SettingsConfigDict"
    human_response: "confirmed"

  - id: "ASSUM-005"
    scenario: "Sensitive fields are masked in string representation"
    assumption: "Password and creds_file should be excluded or masked in repr/str and model_dump output"
    confidence: low
    basis: "Security best practice; prevents accidental secret logging in production"
    human_response: "confirmed"

  - id: "ASSUM-006"
    scenario: "Providing both password auth and creds file is rejected"
    assumption: "Password auth (user/password) and NKey credentials file are mutually exclusive"
    confidence: low
    basis: "Ambiguous auth intent; nats-py uses one authentication method or the other, not both"
    human_response: "confirmed"
