# Assumptions manifest for event-type-schemas.feature
# Generated by /feature-spec

feature: "Event Type Schemas"
generated: "2026-04-08T00:00:00Z"
stack: "python"
review_required: true

assumptions:
  - id: "ASSUM-001"
    scenario: "Build ID follows naming convention"
    assumption: "build_id format is build-{feature_id}-{YYYYMMDDHHMMSS}"
    confidence: high
    basis: "System spec explicitly states this format in BuildProgressPayload documentation"
    human_response: "confirmed"

  - id: "ASSUM-002"
    scenario: "Payload schemas tolerate unknown fields for forward compatibility"
    assumption: "All payload schemas use extra='ignore' for forward compatibility"
    confidence: high
    basis: "ADR XC-schema-versioning mandates extra='ignore' on all models"
    human_response: "confirmed"

  - id: "ASSUM-003"
    scenario: "Agent max_concurrent of zero is rejected"
    assumption: "max_concurrent minimum is 1"
    confidence: medium
    basis: "An agent with max_concurrent=0 cannot accept any work; domain logic requires at least 1"
    human_response: "confirmed"

  - id: "ASSUM-004"
    scenario: "Heartbeat rejects negative queue depth and active tasks"
    assumption: "queue_depth and active_tasks have ge=0 constraint"
    confidence: medium
    basis: "Negative counts are nonsensical for queue depth and active task tracking"
    human_response: "confirmed"

  - id: "ASSUM-005"
    scenario: "BuildCompletePayload task counts must be internally consistent"
    assumption: "Schema enforces tasks_completed + tasks_failed == tasks_total"
    confidence: medium
    basis: "Inconsistent counts would mislead dashboards and observers; producer bug detection"
    human_response: "confirmed"

  - id: "ASSUM-006"
    scenario: "FeaturePlannedPayload wave_count must match waves list length"
    assumption: "Schema enforces wave_count == len(waves)"
    confidence: medium
    basis: "Mismatch between declared count and actual list length indicates a producer bug"
    human_response: "confirmed"

  - id: "ASSUM-007"
    scenario: "Payload handles excessively long string fields"
    assumption: "No max_length constraint on string fields in payload schemas"
    confidence: low
    basis: "System spec does not define string length limits for any payload field"
    human_response: "confirmed"

  - id: "ASSUM-008"
    scenario: "Agent registration rejects invalid agent_id format"
    assumption: "agent_id must be a kebab-case identifier (pattern: ^[a-z][a-z0-9-]*$)"
    confidence: low
    basis: "Convention inferred from spec examples (product-owner-agent, ideation-agent); not explicitly stated"
    human_response: "confirmed"
