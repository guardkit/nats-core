# Assumptions manifest for nats-client.feature
# Generated by /feature-spec

feature: "NATS Client"
generated: "2026-04-08T00:00:00Z"
stack: "python"
review_required: false

assumptions:
  - id: "ASSUM-001"
    scenario: "Deeply nested topic template resolves correctly"
    assumption: "Approval topics have the deepest nesting at 5 segments: agents.approval.{agent_id}.{task_id}.response"
    confidence: medium
    basis: "Derived from Topics registry in system spec — approval response is the deepest template"
    human_response: "confirmed"

  - id: "ASSUM-002"
    scenario: "Agent tool call times out when no reply is received"
    assumption: "Tool call request-reply timeout defaults to 30 seconds"
    confidence: medium
    basis: "Common RPC convention; nats-py default request timeout"
    human_response: "confirmed"

  - id: "ASSUM-003"
    scenario: "Creating a client with an empty source ID is rejected"
    assumption: "URL validation scheme must be nats:// or tls:// only"
    confidence: medium
    basis: "nats-py convention; already specified in nats-configuration.feature"
    human_response: "confirmed"

  - id: "ASSUM-004"
    scenario: "Client creation validates auth config"
    assumption: "Partial auth (user without password or vice versa) is an error"
    confidence: low
    basis: "Already specified in nats-configuration.feature; applies to client creation too"
    human_response: "confirmed"
